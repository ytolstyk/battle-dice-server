# Battle Dice Express Server

Simple express server using web sockets for the battle dice game.

## How to use

Clone the repo, run `npm install`, then `npm run start` to initialize it. Change the port if necessary in `server.ts`.
